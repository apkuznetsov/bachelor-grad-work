@model IEnumerable<Webapp.Models.ExperimentNameViewModel>

@{
    ViewData["Title"] = "Список экспериментов";
}

<h1>Список экспериментов</h1>

<p>
    <a asp-action="Create">Создать новый эксперимен</a>
</p>
<table class="table">
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a asp-action="Details" asp-route-id="@item.ExperimentId">
                        @Html.DisplayFor(modelItem => item.Name)
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
